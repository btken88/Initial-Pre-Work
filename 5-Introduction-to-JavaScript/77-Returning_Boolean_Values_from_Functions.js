//I learned you can use comparisons within a function to return a boolean rather than using if/else statements. This can help keep code simpler
function isLess(a, b) {
  // Fix this code
  return a<b;
}

// Change these values to test
isLess(10, 15);
